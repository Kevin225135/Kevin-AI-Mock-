# Required. The app uses PostgreSQL only; there is no in-memory fallback.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ai_mock_coach?schema=public"

# Required for JWT access tokens and refresh-session signing.
# Use a long random value in every deployed environment.
AUTH_JWT_SECRET="replace-with-at-least-32-random-characters"
AUTH_ACCESS_TOKEN_TTL_MINUTES="15"
AUTH_REFRESH_TOKEN_TTL_DAYS="30"
DEFAULT_USER_PLAN="FREE"

# Optional seed admin. Set both values before running npm run db:seed.
SEED_ADMIN_EMAIL=""
SEED_ADMIN_PASSWORD=""

# Optional. If omitted, V1 runs scoring synchronously in the Next.js API route.
REDIS_URL="redis://localhost:6379"
# Set both REDIS_URL and this flag to move scoring to the BullMQ worker.
ASYNC_SCORING="false"

# Optional server-error webhook (Sentry relay, Slack-compatible gateway, etc.).
ERROR_WEBHOOK_URL=""

# OpenAI-compatible Qwen text provider.
AI_PROVIDER="openai-compatible"
AI_API_BASE_URL="https://dashscope.aliyuncs.com/compatible-mode/v1"
AI_API_KEY=""
AI_MODEL="qwen3.5-plus"
DASHSCOPE_REALTIME_MODEL="qwen3.5-omni-plus-realtime"
DASHSCOPE_REALTIME_URL="wss://dashscope.aliyuncs.com/api-ws/v1/realtime"

# Volcengine Ark Responses API provider.
# Set AI_PROVIDER="ark" and put the real key in .env.local, not in git.
ARK_API_BASE_URL="https://ark.cn-beijing.volces.com/api/v3"
ARK_API_KEY=""
RAG_LLM_ENABLED="true"
# Set false if the selected provider account has not enabled web search.
RAG_WEB_SEARCH_ENABLED="true"
RAG_PROVIDER_TIMEOUT_MS="8000"
ARK_MODEL="doubao-seed-2-0-pro-260215"

# Hybrid RAG. text-embedding-v4 supports Chinese/English and batches up to 10.
# EMBEDDING_API_KEY falls back to AI_API_KEY when omitted.
EMBEDDING_PROVIDER="dashscope"
EMBEDDING_API_BASE_URL="https://dashscope.aliyuncs.com/compatible-mode/v1"
EMBEDDING_API_KEY=""
EMBEDDING_MODEL="text-embedding-v4"
EMBEDDING_DIMENSIONS="1024"
EMBEDDING_BATCH_SIZE="10"
EMBEDDING_TIMEOUT_MS="12000"

# Optional cross-encoder reranker. When URL/key are absent, the app uses a
# deterministic local reranker and keeps the interview flow available.
RERANK_ENABLED="true"
RERANK_API_URL="https://dashscope.aliyuncs.com/api/v1/services/rerank/text-rerank/text-rerank"
RERANK_API_KEY=""
RERANK_MODEL="qwen3-rerank"
RERANK_TIMEOUT_MS="8000"
